#spring:
#  application:
#    name: item-service

#eureka:
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#  instance:
#    instance-id: ${spring.application.name}-${random.int}

    
server:
  port: 8080